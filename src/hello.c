// Importação da biblioteca padrão para o uso de teclado e monitor //
#include <stdio.h>

int main(){
    // para exibir uma mensagem na tela iremos usar o comando printf (exibir formatando) //
    printf("Hello, World!\n");
    // informar a o compilador que o programa acabou //
    return 0;
}